import java.time.LocalDate;

class FilmPhoto extends Photo {
    public FilmPhoto(String title, String series, String captureLocation, LocalDate captureDate) {
        super(title, series, captureLocation, captureDate);
    }
}