import java.time.LocalDate;

class DigitalPhoto extends Photo {
    public DigitalPhoto(String title, String series, String captureLocation, LocalDate captureDate) {
        super(title, series, captureLocation, captureDate);
    }
}