const calculadora = (v1, v2, callback) => callback(v1,v2);

const soma = (v1, v2) => v1 + v2;
const subtracao = (v1, v2) => v1 - v2;

console.log(calculadora(4, 2, soma);
console.log(calculadora(4, 2, subtracao);
