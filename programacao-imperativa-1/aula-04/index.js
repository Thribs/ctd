const fullName = "Ana Paula";
const age = 26;
const mass = 55.0;
const height = 1.62;
const insurance = "prata"

const bmi = mass / (height * height);

console.log(`${fullName} tem ${age} anos de imade e seu IMC é ${bmi}`);